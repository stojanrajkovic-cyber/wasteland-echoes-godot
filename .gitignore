# Godot 4+ editor/import cache - regenerated automatically, machine-specific.
# Never commit this; it causes merge-conflict noise and bloats the repo.
.godot/

# macOS
.DS_Store

# iOS export artifacts (should never be exported into this folder, but just in case)
*.ipa
*.xcarchive
*.xcodeproj/
*.xcframework/
ExportOptions.plist
DistributionSummary.plist
Packaging.log
PrivacyInfo.xcprivacy